# 감정 분석 시스템 핵심 소스코드 (실제 구현)
## 상세 주석과 함께

---

## **1. 기본 감정 분석 시스템 (Wav2Vec2 기반)**

```python
# emotion_server.py - 실제 구현된 감정 분석 클래스
class EmotionAnalyzer:
    """Wav2Vec2 기반 고성능 음성 감정 분석기"""
    
    def __init__(self):
        self.model = None
        self.feature_extractor = None
        self.device = torch.device("cuda" if torch.cuda.is_available() else "cpu")
        
        # 🎯 Wav2Vec2 모델의 감정 라벨 (7가지 기본 감정)
        self.emotion_labels = ["angry", "disgust", "fear", "happy", "neutral", "sad", "surprise"]
        self.load_model()

    def load_model(self):
        """🤖 Wav2Vec2 감정 분석 모델 초기화"""
        try:
            # Facebook의 사전 훈련된 모델 사용
            model_name = "Dpngtm/wav2vec2-emotion-recognition"
            self.model = Wav2Vec2ForSequenceClassification.from_pretrained(model_name).to(self.device)
            self.feature_extractor = AutoFeatureExtractor.from_pretrained(model_name)
            logger.info("모델 로딩 성공")
        except Exception as e:
            logger.error(f"모델 로딩 실패: {e}")
            raise

    def preprocess_audio(self, audio_data: bytes) -> np.ndarray:
        """🎤 오디오 데이터 전처리 (Wav2Vec2 모델 호환)"""
        try:
            if not audio_data or len(audio_data) == 0:
                logger.warn("빈 오디오 데이터")
                return None
            
            # 바이트 데이터를 numpy 배열로 변환
            audio_bytes = io.BytesIO(audio_data)
            
            # soundfile로 오디오 읽기 (다양한 형식 지원)
            try:
                audio_array, sample_rate = sf.read(audio_bytes)
            except Exception as e:
                logger.error(f"오디오 파일 읽기 실패: {e}")
                return None
            
            # 스테레오 → 모노 변환
            if len(audio_array.shape) > 1:
                audio_array = np.mean(audio_array, axis=1)
            
            # 16kHz로 리샘플링 (Wav2Vec2 모델 요구사항)
            if sample_rate != 16000:
                import librosa
                audio_array = librosa.resample(audio_array, orig_sr=sample_rate, target_sr=16000)
            
            # 정규화 ([-1, 1] 범위)
            if audio_array.max() > 1.0 or audio_array.min() < -1.0:
                audio_array = audio_array / np.max(np.abs(audio_array))
            
            return audio_array
            
        except Exception as e:
            logger.error(f"오디오 전처리 오류: {e}")
            return None

    def analyze_emotion(self, audio_data: bytes) -> Dict[str, Any]:
        """
        🎭 핵심 감정 분석 함수
        - Wav2Vec2 모델을 사용한 음성 감정 인식
        - 한국어 최적화 한계를 인정하고 기본 분석 제공
        """
        start_time = time.time()
        
        try:
            # ✅ 1단계: 오디오 전처리
            audio_array = self.preprocess_audio(audio_data)
            if audio_array is None:
                return {
                    'error': '오디오 전처리 실패',
                    'success': False
                }
            
            # ✅ 2단계: 특성 추출 (Wav2Vec2 피처 추출기 사용)
            inputs = self.feature_extractor(
                audio_array, 
                sampling_rate=16000, 
                return_tensors="pt", 
                padding=True
            )
            
            # GPU 메모리로 이동
            inputs = {key: value.to(self.device) for key, value in inputs.items()}
            
            # ✅ 3단계: 모델 추론 (그라디언트 계산 비활성화로 성능 향상)
            with torch.no_grad():
                outputs = self.model(**inputs)
                predictions = torch.nn.functional.softmax(outputs.logits, dim=-1)
            
            # ✅ 4단계: 결과 해석
            predictions_np = predictions.cpu().numpy()[0]
            
            # 각 감정별 확률 계산
            emotion_scores = {}
            for i, emotion in enumerate(self.emotion_labels):
                emotion_scores[emotion] = float(predictions_np[i])
            
            # 최고 확률 감정 선택
            predicted_emotion_idx = np.argmax(predictions_np)
            predicted_emotion = self.emotion_labels[predicted_emotion_idx]
            confidence = float(predictions_np[predicted_emotion_idx])
            
            processing_time = time.time() - start_time
            
            # ✅ 5단계: 결과 반환
            return {
                'emotion': predicted_emotion,
                'confidence': confidence,
                'emotion_scores': emotion_scores,
                'processing_time': processing_time,
                'model_info': 'Dpngtm/wav2vec2-emotion-recognition',
                'korean_optimized': False,  # 🚨 한계 명시
                'success': True
            }
            
        except Exception as e:
            logger.error(f"감정 분석 오류: {e}")
            return {
                'error': str(e),
                'success': False
            }
```

---

## **2. 감정 기반 응답 생성 시스템**

```python
# emotion_server.py - 감정별 맞춤 응답 생성
def generate_emotion_response(emotion: str, confidence: float) -> str:
    """
    🎭 감정에 따른 자연스러운 응답 생성
    - 감정별 다양한 응답 패턴 제공
    - 신뢰도 기반 응답 조정
    """
    
    # ✅ 1단계: 신뢰도 검증 (낮은 신뢰도 처리)
    if confidence < 0.3:
        return "음성을 명확히 인식하지 못했습니다. 다시 말씀해주세요."
    
    # ✅ 2단계: 감정별 응답 패턴 정의
    responses = {
        'angry': [
            "화가 나신 것 같아요. 마음을 진정시킬 수 있는 음악을 틀어드릴까요?",
            "분노가 느껴져요. 심호흡을 해보는 건 어떨까요?"
        ],
        'happy': [
            "기분이 좋아 보이시네요! 좋은 하루 되세요!",
            "행복해 보이시네요! 계속 좋은 일만 가득하길 바랍니다!"
        ],
        'sad': [
            "슬퍼 보이시네요. 위로가 되는 음악을 들려드릴까요?",
            "마음이 힘드신가요? 잠시 쉬어가도 괜찮아요."
        ],
        'neutral': [
            "어떻게 도와드릴까요?",
            "필요하신 게 있으신가요?"
        ],
        'fear': [  # 'fearful' → 'fear'로 수정 (실제 모델 라벨)
            "불안해 보이시네요. 안정감을 주는 음악을 틀어드릴까요?"
        ],
        'disgust': [
            "불쾌감을 느끼신 것 같아요. 기분 전환이 필요하신가요?"
        ],
        'surprise': [  # 'surprised' → 'surprise'로 수정 (실제 모델 라벨)
            "놀라신 것 같아요. 괜찮으신가요?"
        ]
    }
    
    # ✅ 3단계: 랜덤 응답 선택 (자연스러운 대화)
    import random
    emotion_responses = responses.get(emotion, ["어떻게 도와드릴까요?"])
    return random.choice(emotion_responses)
```

---

## **3. 실제 감정 분석 API 엔드포인트**

```python
# emotion_server.py - 실제 구현된 API
@app.route('/analyze_emotion', methods=['POST'])  # POST 요청을 받는 Flask 엔드포인트
def analyze_emotion():
    """
    🚀 음성 감정 분석 API 메인 함수
    - 클라이언트가 보낸 음성 데이터를 받아서 감정을 분석하고 결과를 반환
    - Base64로 인코딩된 오디오 파일을 받아서 처리
    - Wav2Vec2 AI 모델로 7가지 감정(화남, 기쁨, 슬픔 등)을 판단
    """
    try:
        # ✅ 1단계: 클라이언트가 보낸 데이터가 올바른지 확인
        # JSON 형태로 데이터가 왔는지, 'audio_base64' 필드가 있는지 검사
        if not request.json or 'audio_base64' not in request.json:
            # 잘못된 요청이면 400 에러와 함께 에러 메시지 반환
            return jsonify({
                'error': 'audio_base64 필드가 필요합니다.',  # 사용자에게 알려줄 에러 내용
                'success': False  # 처리 실패 표시
            }), 400  # HTTP 400 Bad Request 상태 코드
        
        # 클라이언트가 보낸 Base64로 인코딩된 오디오 데이터 추출
        audio_base64 = request.json['audio_base64']
        # Base64 문자열을 실제 바이너리 오디오 데이터로 변환
        audio_data = base64.b64decode(audio_base64)
        
        # 오디오 데이터가 실제로 있는지 확인 (빈 파일 체크)
        if len(audio_data) == 0:
            return jsonify({
                'error': '오디오 데이터가 비어있습니다.',
                'success': False
            }), 400

        # ✅ 2단계: 실제 AI 모델을 사용한 감정 분석 수행
        # EmotionAnalyzer 클래스의 analyze_emotion 메서드 호출
        # 이 함수가 Wav2Vec2 모델을 사용해서 감정을 분석함
        result = emotion_analyzer.analyze_emotion(audio_data)
        
        # 감정 분석에 실패했다면 500 에러 반환
        if not result['success']:
            return jsonify(result), 500  # HTTP 500 Internal Server Error
        
        # ✅ 3단계: 분석된 감정에 맞는 응답 메시지 생성
        emotion = result['emotion']        # 분석된 감정 (예: 'happy', 'sad', 'angry')
        confidence = result['confidence']  # 분석 신뢰도 (0~1 사이 값)
        # 감정에 맞는 적절한 응답 메시지 생성 (예: "기분이 좋아보이시네요!")
        response_text = generate_emotion_response(emotion, confidence)
        
        # ✅ 4단계: 클라이언트에게 보낼 최종 결과 데이터 구성
        final_result = {
            'emotion': emotion,                    # 감지된 주요 감정
            'confidence': confidence,              # 해당 감정의 신뢰도 (얼마나 확실한지)
            'response': response_text,             # 감정에 맞는 맞춤 응답 메시지
            'emotion_scores': result['emotion_scores'],  # 7가지 감정 각각의 점수
            'processing_time': result['processing_time'], # 분석에 걸린 시간 (초)
            'success': True,                       # 성공적으로 처리됨을 표시
            'korean_optimized': False,             # 한국어 최적화 안됨을 솔직히 표시
            'model_info': 'Dpngtm/wav2vec2-emotion-recognition'  # 사용한 AI 모델 정보
        }
        
        # JSON 형태로 결과를 클라이언트에게 반환
        return jsonify(final_result)
        
    except Exception as e:
        # 예상치 못한 오류가 발생한 경우 로그에 기록하고 에러 응답 반환
        logger.error(f"API 오류: {e}")
        return jsonify({
            'error': str(e),    # 오류 내용을 문자열로 변환해서 전달
            'success': False
        }), 500  # HTTP 500 Internal Server Error
```

---

## **4. 텍스트 기반 감정 분석 시스템 (conversation.js)**

```javascript
// conversation.js - 실제 구현된 텍스트 감정 분석
const analyzeEmotion = (text) => {
  /**
   * 📝 텍스트 기반 감정 분석 (패턴 매칭)
   * - 한국어 감정 키워드 패턴 인식
   * - 음성 감정 분석과 보완적 역할
   */
  const lowerText = text.toLowerCase();
  
  // ✅ 한국어 감정 패턴 정의
  const emotionPatterns = {
    happy: {
      pattern: /(좋아|행복|즐거워|신나|기쁘|만족|감사|사랑)/,
      intensity: 0.8
    },
    sad: {
      pattern: /(슬퍼|우울|짜증|걱정|힘들|지침)/,
      intensity: 0.7
    },
    angry: {
      pattern: /(화나|짜증|분노|답답|짜증나)/,
      intensity: 0.9
    },
    fearful: {
      pattern: /(무서워|불안|걱정|두려워|겁나)/,
      intensity: 0.6
    },
    neutral: {
      pattern: /(보통|괜찮|그냥|평범|무덤덤)/,
      intensity: 0.5
    }
  };

  let maxEmotion = 'neutral';
  let maxIntensity = 0;
  const probabilities = {};

  // ✅ 각 감정에 대한 확률 계산
  for (const [emotion, data] of Object.entries(emotionPatterns)) {
    const matches = (lowerText.match(data.pattern) || []).length;
    const probability = matches ? data.intensity * (matches / lowerText.split(' ').length) : 0;
    probabilities[emotion] = probability;

    if (probability > maxIntensity) {
      maxIntensity = probability;
      maxEmotion = emotion;
    }
  }

  return {
    emotion: maxEmotion,
    probability: probabilities,
    intensity: maxIntensity
  };
};
```

---

## **5. 음성 인식 + 감정 분석 통합 시스템 (speech.js)**

```javascript
// speech.js - 실제 구현된 음성 처리 파이프라인
const convertAudioToText = async (audioBuffer) => {
  /**
   * 🎤 한국어 음성을 텍스트로 변환하는 함수
   * - 사용자가 말한 음성을 ETRI API로 보내서 한글 텍스트로 받아옴
   * - 예: "미러야 날씨 알려줘" 음성 → "미러야 날씨 알려줘" 텍스트
   */
  try {
    // ✅ 1단계: 음성 데이터를 인터넷으로 전송 가능한 형태로 변환
    // Buffer 형태의 음성 데이터를 Base64 문자열로 인코딩 (API 전송용)
    const audioBase64 = audioBuffer.toString('base64');
    
    // ETRI API에 보낼 요청 데이터 구성
    const requestBody = {
      request_id: "reserved field",        // ETRI에서 요구하는 예약 필드
      argument: {
        language_code: "korean",           // 🇰🇷 음성 언어를 한국어로 설정
        audio: audioBase64                 // Base64로 변환된 음성 데이터
      }
    };

    // ✅ 2단계: ETRI 음성인식 API에 HTTP POST 요청 보내기
    const response = await axios.post(ETRI_API_URL, requestBody, {
      headers: {
        'Content-Type': 'application/json; charset=UTF-8',  // JSON 데이터임을 명시
        'Authorization': ETRI_API_KEY      // API 사용 권한 인증키
      },
      timeout: 10000                      // 10초 내에 응답 안오면 타임아웃
    });

    // ✅ 3단계: ETRI에서 받은 응답 데이터 처리
    // result가 0이면 성공, return_object에 인식된 텍스트가 들어있음
    if (response.data.result === 0 && response.data.return_object) {
      // 인식된 한국어 텍스트 추출 (없으면 빈 문자열)
      const recognizedText = response.data.return_object.recognized || '';
      
      // 핫워드("미러야") 대기 중이 아닐 때만 성공 로그 출력
      // (핫워드 대기 중에는 너무 많은 로그가 찍히는 것을 방지)
      if (hotwordMode !== 'hotword') {
        log.info('ETRI 음성인식 성공:', recognizedText);
      }
      return recognizedText;  // 인식된 텍스트 반환
    } else {
      // 음성인식 실패한 경우 에러 로그 출력
      log.error('ETRI 음성인식 실패:', response.data);
      return null;  // 실패를 나타내는 null 반환
    }
  } catch (error) {
    // ✅ 4단계: 네트워크 오류나 API 제한 등의 예외 상황 처리
    if (error.response) {
      const status = error.response.status;  // HTTP 상태 코드 확인
      
      if (status === 429) {
        // 429: 너무 많은 요청을 보내서 API 제한에 걸린 경우
        log.warn('ETRI API 동시 요청 제한 도달. 잠시 대기 후 재시도합니다.');
        // 3초 대기 후 다시 시도할 수 있도록 함
        await new Promise(resolve => setTimeout(resolve, 3000));
        return null;
      } else if (status === 403) {
        // 403: API 키가 잘못되었거나 일일 사용량 초과
        log.error('ETRI API 인증 실패 또는 일일 제한 초과');
        return null;
      }
    }
    
    // 기타 모든 오류 (네트워크 문제, 서버 오류 등)
    log.error('ETRI 음성인식 API 오류:', error.message);
    return null;
  }
};

// 실제 음성 처리 파이프라인 (핵심 부분)
const startContinuousHotwordListener = (processRecognizedCommand, broadcast, dependencies = null) => {
  /**
   * 🔄 24시간 계속 듣고 있는 음성 인식 시스템
   * - 마이크로 계속 소리를 듣다가 "미러야"라는 핫워드를 감지
   * - 핫워드 감지 → 사용자 명령 인식 → 감정 분석 → 적절한 응답 생성
   * - 예: "미러야" (핫워드) → "날씨 알려줘" (명령) → 감정분석 → "좋은 기분이시네요! 오늘 날씨는..."
   */
  
  // ... (중간의 복잡한 음성 인식 로직들 생략) ...
  
  // ✅ 사용자가 실제 명령을 말했을 때 처리하는 부분
  if (cleanCommand) {  // cleanCommand: "미러야" 제거하고 정리된 실제 명령어
    log.info('사용자 명령 인식됨:', cleanCommand);  // 예: "날씨 알려줘"
    
    try {
      // 🎭 수집된 음성 데이터로 감정 분석 수행
      let emotionResult = null;  // 감정 분석 결과를 저장할 변수
      
      // 감정 분석용 음성 데이터가 수집되었는지 확인
      if (isCollectingEmotionAudio && emotionAudioBuffer.length > 0) {
        log.info('🎯 전체 음성으로 감정 분석 시작');
        
        // 여러 개의 음성 청크를 하나로 합치기 (전체 대화 음성)
        const fullAudioBuffer = Buffer.concat(emotionAudioBuffer);
        
        // 감정 분석을 위해 Python 서버(포트 5050)로 음성 데이터 전송
        const audioBase64 = fullAudioBuffer.toString('base64');  // Base64로 인코딩
        const emotionResponse = await axios.post('http://localhost:5050/analyze_emotion', {
          audio_base64: audioBase64  // 위에서 만든 API로 감정 분석 요청
        }, { timeout: 15000 });  // 15초 타임아웃 (감정 분석은 시간이 좀 걸림)
        
        // 감정 분석이 성공했다면 결과 데이터 정리
        if (emotionResponse.data && emotionResponse.data.success) {
          emotionResult = {
            emotion: emotionResponse.data.emotion,          // 감지된 감정 (예: 'happy', 'sad')
            confidence: emotionResponse.data.confidence,    // 감정 분석 신뢰도 (0~1)
            intensity: emotionResponse.data.confidence,     // 감정 강도 (신뢰도와 동일하게 설정)
            korean_optimized: false                         // 한국어 최적화 안됨을 명시
          };
          // 감정 분석 결과를 로그로 출력 (소수점 둘째자리까지)
          log.info(`🎭 감정 분석 결과: ${emotionResult.emotion} (신뢰도: ${emotionResult.confidence:.2f})`);
        }
      }
      
      // 🤖 실제 명령 처리 (감정 데이터도 함께 전달)
      if (processRecognizedCommand) {
        // 음성 텍스트, 시스템 의존성, 감정 데이터를 모두 전달해서 명령 처리
        // 이 함수가 "날씨 알려줘" + 감정 정보를 받아서 적절한 응답을 생성함
        await processRecognizedCommand(cleanCommand, dependencies, emotionResult);
      }
      
    } catch (error) {
      // 감정 분석이나 명령 처리 중 오류 발생 시 로그 출력
      log.error('명령 처리 중 오류:', error);
    }
  }
};
```
